import DashboardOverview from "@/components/developer/dashboard-overview";

export default function DeveloperProjectPage() {
  // Sharing the same overview layout for the project page, as it contains the projects list
  return <DashboardOverview />;
}
